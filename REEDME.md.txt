#ft_printf

c言語のprintf関数の再現（最小フィールド値などの一部の機能は未実装）

printf関数とは文字を入力するとそれを標準出力（モニターなど）に表示する関数
また第二引数以降には文字を以外にも数字やポインタなどいろいろな値を入れることができ
第一引数の所定の場所に挿入して表示できる

使用している関数は（自作関数とmallocとfreeとwriteのみ）

#get_next_line

fileを読み込みそれを１行ずつ　表示させる関数
使用している関数は（自作関数とmallocとfreeとreadのみ）
